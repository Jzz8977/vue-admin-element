/* eslint-disable no-undef */
// 光源基类 
THREE.Light = function(color) {
  // 啊
}

// 环境光
THREE.AmbientLight(环境光)

// 区域光
THREE.AreaLight(区域光)

// 方向光
THREE.DirectionalLight(方向光)

// 聚光灯
THREE.SpotLight(聚光灯)

// 点光源
THREE.Pointe(点光源)
